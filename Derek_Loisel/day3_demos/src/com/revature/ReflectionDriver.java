package com.revature;

public class ReflectionDriver {
	public static void main(String[] args) {
		/*
		 * reflection provides ability to inspect and modify the runtime behavior of an application.  it can look at itself and inspect things like classes, interfaces, enums, fields , methods, constructors, etc. within itself
		 */
		
		//wezleys example of changing the string class access modifiers and making strings changeable and changing the value of the string
}
}
